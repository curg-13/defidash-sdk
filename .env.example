# DefiDash SDK - Example Environment Variables
# Copy this to .env.test and fill in your values

# ============================================================================
# Required
# ============================================================================

# Sui wallet secret key (base64 encoded)
SECRET_KEY=YOUR_SECRET_KEY_HERE

# ============================================================================
# Network (Optional)
# ============================================================================

# Sui RPC URL (defaults to mainnet)
# SUI_FULLNODE_URL=https://fullnode.mainnet.sui.io:443

# ============================================================================
# Strategy Settings
# ============================================================================

# Lending protocol: "suilend" or "navi"
LEVERAGE_PROTOCOL=suilend

# ============================================================================
# Deposit Configuration (Choose ONE option)
# ============================================================================

# Deposit asset: symbol (LBTC, SUI, ETH) or full coin type
LEVERAGE_DEPOSIT_COIN_TYPE=LBTC

# Option 1: Deposit amount in raw units (e.g., 1000 = 0.00001 LBTC for 8 decimals)
# For $1 worth of LBTC: amount = ($1 / LBTC_price) × 10^8
# Example: If LBTC = $100,000, then $1 = 1,000 raw units
LEVERAGE_DEPOSIT_AMOUNT=1000

# Option 2: Deposit value in USD (e.g., 1.0 for $1 worth of the asset)
# The SDK will automatically convert this to the appropriate amount based on current price
# Uncomment to use USD value instead of raw amount:
# LEVERAGE_DEPOSIT_VALUE_USD=1.0

# Note: Set ONLY ONE of the above options. If both are set, the SDK will return an error.

# Leverage multiplier (e.g., 1.5, 2.0, 3.0)
LEVERAGE_MULTIPLIER=2

# ============================================================================
# Execution Mode
# ============================================================================

# Transaction mode: "dryrun" (simulate) or "exec" (real transaction)
TX_MODE=dryrun
